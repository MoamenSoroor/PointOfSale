using PointOfSale.Application.Contracts.Infrastructure.Files;
using PointOfSale.Application.Models.Files;

namespace PointOfSale.Infrastructure.Files
{
    //public class StorageClientFactory
    //{
    //    private readonly List<(FileStorageType storageType, Func<IStorageClient> clientStorageResolver)> storageClients =
    //new List<(FileStorageType storageType, Func<IStorageClient> clientStorageResolver)>();
        
    //    private readonly IServiceProvider provider;

    //    public StorageClientFactory(IServiceProvider provider)
    //    {
    //        this.provider = provider;
    //    }
    //}


}
